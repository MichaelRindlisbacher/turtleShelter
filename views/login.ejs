<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Turtle Shelter Project</title>
    <link rel="stylesheet" type="text/css" href="/styles/eventrequest.css">     
    <style>
        /* General page styles */
        body.general-page {
            font-family: 'Arial', sans-serif;
            background: #D3D3D3;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* Login container */
        .login-container {
            display: flex;
            justify-content: center;
            align-items: center;
            width: 100%;
            padding: 20px;
        }

        /* Login box styling */
        .login-box {
            background: #fff;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
            width: 100%;
            max-width: 375px;
            padding: 30px;
            text-align: center;
            animation: fadeIn 0.8s ease-in-out;
        }

        /* Title styling */
        .login-title {
            font-size: 1.8em;
            color: #1a1a1a;
            margin-bottom: 20px;
        }

        /* Label styling */
        .login-label {
            display: block;
            font-size: 0.9em;
            font-weight: bold;
            margin-top: 15px;
            text-align: left;
        }

        /* Input styling */
        .login-input {
            width: 100%;
            padding: 10px;
            margin-top: 5px;
            border: 1px solid #ccc;
            border-radius: 5px;
            font-size: 1em;
            box-sizing: border-box;
        }

        /* Button styling */
        .login-button {
            background: #1ABC9C;
            color: white;
            border: none;
            border-radius: 5px;
            padding: 10px 15px;
            font-size: 1em;
            margin-top: 20px;
            cursor: pointer;
            width: 100%;
            transition: background-color 0.3s ease;
        }

        .login-button:hover {
            background: #0097A7;
        }

        /* Box shadow and hover effects */
        .login-box:hover {
            box-shadow: 0 6px 14px rgba(0, 0, 0, 0.3);
        }

        /* Animation for the fade-in effect */
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: scale(0.9);
            }
            to {
                opacity: 1;
                transform: scale(1);
            }
        }

        /* Responsive Design */
        @media (max-width: 480px) {
            .login-box {
                width: 90%;
                padding: 20px;
            }

            .login-title {
                font-size: 1.5em;
            }
        }
    </style>
</head>
<body class="general-page">
    <header>
        <%- include('partials/navbar.ejs') %>
      </header>
    <div class="login-container">
        <div class="login-box">
            <h2 class="login-title">Admin Login</h2>
            <form action="/login" method="post">
                <label for="username" class="login-label">Username</label>
                <input type="text" id="username" name="username" class="login-input" required>

                <label for="password" class="login-label">Password</label>
                <input type="password" id="password" name="password" class="login-input" required>

                <button type="submit" class="login-button">Login</button>
            </form>
        </div>
    </div>
    <script src="../public/scripts/script.js"></script>
</body>
</html>